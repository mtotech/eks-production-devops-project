apiVersion: v1
kind: Service

metadata:
  name: flask-app
  namespace: production

spec:

  selector:
    app: flask-app
    version: blue

  ports:

  - port: 5000

    targetPort: 5000

  type: {{ .Values.service.type }}
